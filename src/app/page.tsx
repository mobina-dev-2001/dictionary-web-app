import { Header } from '@/components/header';

// ----------------------------------------------------------------------

export default function Home() {
  return (
    <div className="grid place-items-center px-[clamp(1.5rem,6vw,2.5rem)] py-[clamp(1.5rem,6vw,3.625rem)]">
      <Header />
    </div>
  );
}
